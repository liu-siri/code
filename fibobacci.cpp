#include<stdio.h>
#include<math.h>
int main()
{
	int n=0;
	double fn=0,left=0,right=0;
	scanf("%d",&n);
	left=pow(((1.0+sqrt(5)/2.0),(double)n);
	right=pow(((1.0-sqrt(5))/2.0),(double)n);
	fn=(left-right)/sqrt(5);
	printf("%.2llf",fn);
	return 0;
 } 
